<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title><%= title %></title>
    <link rel="stylesheet" href="/stylesheets/output.css" />
  </head>
  <body class="bg-gray-40">
    <% if (!user) { %>
    <nav class="bg-white border-b">
      <div class="mx-auto max-w-7xl sm:px-6 lg:px-8">
        <div class="relative flex h-16 items-center justify-between">
          <div>
            <h2 class="font-bold px-3 text-2xl cursor-default text-emerald-700">
              File Uploader
            </h2>
          </div>
          <div class="hidden sm:block flex justify-center">
            <a
              href="/"
              class="px-3 py-2 rounded-lg block font-medium text-gray-900 hover:bg-gray-200 hover:text-black"
              >Overview</a
            >
          </div>
          <div
            class="absolute inset-y-0 right-0 flex gap-2 items-center pr-2 sm:static sm:inset-auto sm:ml-6 sm:pr-0"
          >
            <a
              href="/login"
              class="px-3 py-2 rounded-lg block font-medium text-gray-900 hover:bg-gray-200 hover:text-black"
              >Log In</a
            >
            <a
              href="/signup"
              class="px-3 py-2 rounded-lg block font-medium bg-emerald-600 text-white hover:bg-emerald-700"
              >Sign Up</a
            >
          </div>
        </div>
      </div>
    </nav>
    <% } else { %>
    <aside
      class="flex flex-col w-64 h-screen overflow-y-auto bg-white border-r items-start py-6"
    >
      <h2 class="font-bold px-3 text-2xl cursor-default text-emerald-700">
        File Uploader
      </h2>
    </aside>
    <% } %>
    <main><%- body %></main>
  </body>
</html>
